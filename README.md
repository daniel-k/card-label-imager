# Card label imager

Generate printable A4 PDFs of ID-1 card labels. Upload an image, position and scale it inside the card mask, then add multiple cards to a sheet.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
