{
  "name": "card-label-imager",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "npm run build && node scripts/build-single-html.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
